{
  "name": "moryflow-meta",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:website": "pnpm --filter moryflow-meta-website build",
    "build:docs": "pnpm --filter docs build",
    "dev:website": "pnpm --filter moryflow-meta-website dev",
    "dev:docs": "pnpm --filter docs dev"
  },
  "engines": {
    "pnpm": ">=9.0.0"
  }
}
